apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: era5-rechunked
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      # Expanded from 250Gi after rechunk job filled the volume; rook-cephfs allows expansion.
      storage: 700Gi
